sudo: false

language: node_js

node_js:
  - node

services:
  - postgresql

notifications:
  email: false

install:
  - npm install

before_script:
  - yarn global add nyc
  - psql -c 'create database barefoot_nomad;' -U postgres
